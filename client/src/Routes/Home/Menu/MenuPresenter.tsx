import React from "react";
import styled from "styled-components";

const Container = styled.div``;

const MenuPresenter: React.SFC = () => <Container>lalaala</Container>;

export default MenuPresenter;
