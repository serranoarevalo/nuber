enum ConfirmationType {
  email
  phone
  password
}

type Confirmation {
  id: Int!
  userId: Int!
  user: User!
  sent: Boolean!
  key: String!
  type: String!
  createdAt: String!
  updatedAt: String!
}
